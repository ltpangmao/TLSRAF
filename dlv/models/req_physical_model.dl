sec_goal(high_hardware_integrity_corresponding_hardware_support_application_firewall).
is_critical(high_hardware_integrity_corresponding_hardware_support_application_firewall).
sec_attribute(hardware_integrity).
asset(corresponding_hardware).
importance(high).
interval(support_application_firewall).
has_properties(high_hardware_integrity_corresponding_hardware_support_application_firewall,high,hardware_integrity,corresponding_hardware,support_application_firewall).
has(energy_supplier,high_hardware_integrity_corresponding_hardware_support_application_firewall).

sec_goal(high_hardware_availability_corresponding_hardware_support_application_firewall).
is_critical(high_hardware_availability_corresponding_hardware_support_application_firewall).
sec_attribute(hardware_availability).
asset(corresponding_hardware).
importance(high).
interval(support_application_firewall).
has_properties(high_hardware_availability_corresponding_hardware_support_application_firewall,high,hardware_availability,corresponding_hardware,support_application_firewall).
has(energy_supplier,high_hardware_availability_corresponding_hardware_support_application_firewall).

sec_goal(high_hardware_integrity_corresponding_hardware_support_secure_access_layer).
is_critical(high_hardware_integrity_corresponding_hardware_support_secure_access_layer).
sec_attribute(hardware_integrity).
asset(corresponding_hardware).
importance(high).
interval(support_secure_access_layer).
has_properties(high_hardware_integrity_corresponding_hardware_support_secure_access_layer,high,hardware_integrity,corresponding_hardware,support_secure_access_layer).
has(energy_supplier,high_hardware_integrity_corresponding_hardware_support_secure_access_layer).

sec_goal(high_hardware_availability_corresponding_hardware_support_secure_access_layer).
is_critical(high_hardware_availability_corresponding_hardware_support_secure_access_layer).
sec_attribute(hardware_availability).
asset(corresponding_hardware).
importance(high).
interval(support_secure_access_layer).
has_properties(high_hardware_availability_corresponding_hardware_support_secure_access_layer,high,hardware_availability,corresponding_hardware,support_secure_access_layer).
has(energy_supplier,high_hardware_availability_corresponding_hardware_support_secure_access_layer).


